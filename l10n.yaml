# Run dart format on the generated files so `flutter gen-l10n` output always
# passes CI's `dart format --set-exit-if-changed .` (the Dart 3.10 formatter
# ships tall-style only; unformatted generator output would fail the check).
format: true
